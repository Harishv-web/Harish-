- `index.html` is the portfolio home page.
- `free-srvices.html` is the new free-services page.
- `styles.css`, `site.js`, and `tools.js` must remain beside both HTML files.
- `manifest.webmanifest`, `sw.js`, and the profile-photo PNG icons enable the installable PWA/offline experience.

For a GitHub Pages site, use HTTPS (the normal GitHub Pages URL already does). Open the site online once, then use the browser's **Install app** option. After that first visit, pages and browser-only tools work offline. The network speed test needs a live connection, and the QR library is cached after its first online use.

The calendar highlights 15 May in every year. The header uses the same `profile.jpg` image followed by **Harish V**. The mobile profile image uses `object-fit: contain`, so the full image stays visible instead of being cropped.
e
